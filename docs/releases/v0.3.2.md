# v0.3.2 — reliable failure receipts

Status: **published** on 2026-09-08 at [GitHub Release v0.3.2](https://github.com/OthmaneBlial/rustdroid/releases/tag/v0.3.2). Cargo metadata and the published binary are 0.3.2. The tag points to `ce727e89711958fc09daa57ac17d90bf8743e8c3`; the release workflow, checksums, clean-container install and provenance verification passed.

## What is already on main
The release includes the changes listed below, supervised readers, observation after readiness/PID discovery, main-process continuity checks and launch-scoped crash detection. These behaviors have local regressions and hosted Linux/KVM evidence.

- Failed runs after backend selection produce schema-v1 JSON, HTML, JUnit and Markdown receipts with a stable stage, classification and path-free summary.
- Best-effort artifact capture preserves the primary run failure when an additional log source is unavailable.
- HTML receipts escape ABI values correctly.
- SHA-256 receipt encoding is compatible with sha2 0.11; the known digest-vector test passes.
- Rust dependencies and workflow actions were updated. The Expo fixture uses Expo 57.0.20, React 19.2.3 and React Native 0.86.3; its workflow now checks SDK compatibility.

## Publication and verification evidence

Hosted candidate evidence (September 8): `c6c3df9` passed the [API 35 action contract](https://github.com/OthmaneBlial/rustdroid/actions/runs/34202434356) and [fresh-runner tests](https://github.com/OthmaneBlial/rustdroid/actions/runs/34202437067). Its [API 30 host workflow](https://github.com/OthmaneBlial/rustdroid/actions/runs/34202431887) passed success, process exit, Java crash, missing launcher and performance checks. `d1ad34c` passed [Linux archive/checksum/clean-container installation](https://github.com/OthmaneBlial/rustdroid/actions/runs/34202677506) and [locked packaged-source installation](https://github.com/OthmaneBlial/rustdroid/actions/runs/34202677019). The later exact-candidate nine-case validation below supersedes the initial four-case runtime coverage. These are commit-specific gates, not publication proof.

- [x] Resolve and verify the observation-window/reader-lifecycle risks in ROADMAP.md M1. The [nine-case matrix at d9022f9](https://github.com/OthmaneBlial/rustdroid/actions/runs/34205061851) passed, including real ANR, injected reader/marker failures and cleanup precedence, alongside local observer regressions.
- [x] Run the failure matrix and normal fixture on Linux/KVM at the exact candidate revision. [Exact-candidate host validation](https://github.com/OthmaneBlial/rustdroid/actions/runs/34205810640) checked out archive source `2754c3e` and passed the runtime/smoke suite, nine failure-matrix cases and performance gate. Matrix reports were downloaded and inspected.
- [x] Update Cargo.toml/Cargo.lock, changelog and action examples to the tested release. The published tag is `v0.3.2` and the reusable examples pin the tag commit `ce727e89711958fc09daa57ac17d90bf8743e8c3`.
- [x] Build the x86_64 Linux archive and checksum, verify clean-container installation and attach provenance. [Published workflow](https://github.com/OthmaneBlial/rustdroid/actions/runs/34214422456) passed for tag `v0.3.2`; the downloaded release archive SHA-256 is `0617c7ace752cfdb924e68edf8df391c6e76da395a1dba78e97e7bdc77ca0656`, and the fixture SHA-256 is `5006fcae4718a1998dbba7097283792807284c29a7eff79f1d3a7a072492cf60`. `gh attestation verify` identifies source `ce727e89711958fc09daa57ac17d90bf8743e8c3`.
- [x] Record the complete real success/failure emulator demo and attach its raw evidence. [Capture 34209082936](https://github.com/OthmaneBlial/rustdroid/actions/runs/34209082936) and [green FFmpeg export 34210495967](https://github.com/OthmaneBlial/rustdroid/actions/runs/34210495967) are documented in `docs/receipts/product-demo.md`.
- [ ] After publication, download and verify the tagged assets and execute the README quickstart on a clean supported host. Asset download/checksum/version/attestation verification is complete; the Android quickstart receipt is pending the dedicated `published-release-verification` run.

## Validation before publication

On September 8, main at `ce727e8` passed local Rust tests, Clippy with warnings denied, formatting, shell checks, Cargo package verification, cargo-deny and the Expo install/audit/compatibility checks. Emulator-dependent tests run only on GitHub Linux/KVM runners, not on this Mac. These checks complement the published Linux release gates.

## Limits to retain in the published notes

Linux/KVM is the supported runtime. macOS CLI compilation is not macOS emulator support. A successful smoke receipt does not validate business flows or a device matrix. Failures before receipt ownership retain their documented stderr/exit-code limits. Logs can contain application data.

The release is immutable; do not move the `v0.3.2` tag or relabel its binary. Keep the post-publication quickstart checkbox open until the dedicated clean-runner workflow produces its receipt.
